name: Update Dashboard Data

on:
  schedule:
    - cron: '47 4 * * *'  # 4:47 AM UTC / 1:47 AM Brasília — primário (adiantado 2h)
    - cron: '47 6 * * *'  # 6:47 AM UTC / 3:47 AM Brasília — backup 1
    - cron: '47 8 * * *'  # 8:47 AM UTC / 5:47 AM Brasília — backup 2
  workflow_dispatch:

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch and update data from API
        run: node scripts/update-data.js
        env:
          DASHBOARD_USER: ${{ secrets.DASHBOARD_USER }}
          DASHBOARD_PASS: ${{ secrets.DASHBOARD_PASS }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          if git diff --quiet data.json; then
            echo "Nenhuma mudanca detectada"
          else
            git add data.json
            git commit -m "chore: update dashboard data - $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
            git push
          fi
